# packages/atlas-synth

Synthetic company package shell.

No seeded company logic is implemented in Phase 1.

Allowed imports:
- may import `atlas-core`
- should not import `atlas-env-helpdesk`, `atlas-graders`, or app/service packages
