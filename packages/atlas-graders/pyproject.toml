[project]
name = "atlas-graders"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["atlas-core", "atlas-env-helpdesk"]

[tool.uv.sources]
atlas-core = { workspace = true }
atlas-env-helpdesk = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atlas_graders"]
